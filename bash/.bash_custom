#! /usr/bin/env bash

BINARY_PATHS=('')
for binary_path in ${BINARY_PATHS[@]}
do
    export PATH="$PATH:$binary_path"
done

alias clearhistory="history -c && history -w && printf '\033c'"
alias cdo='cd $OLDPWD'
